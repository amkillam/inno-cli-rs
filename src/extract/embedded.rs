//Extracts embedded files from the inno setup script
//The only file we will care about here is CompiledCode.bin, which contains the installer
//PascalScript bytecode, returned from here as Pascal ANSI string.